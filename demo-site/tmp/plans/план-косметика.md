# План: Косметические изменения и оптимизация demo-site

## Контекст

Юридический лендинг «Братья Разумовские и Партнёры» (Next.js 16, React 19, Tailwind CSS v4, Framer Motion).
7 групп улучшений: от удаления swirl-курсора до полной системы тём и нового блока FAQ.
**Важно:** текущая версия пушится в GitHub как тег `v1.0` (резервная копия), все дальнейшие изменения — только локальные коммиты без push.

---

## Ответы на интервью (40 вопросов)

### Группа 1: GitHub и цвета

**В1. Как сохранить текущую версию в GitHub?**
→ Тег v1.0 на текущем коммите. После — больше никаких git push.

**В2. Тёмно-бежевый цвет для белых блоков в тёмной теме?**
→ `#626260` (оливково-бежевый)

**В3. Пастельно-бежевый для чёрных блоков в светлой теме?**
→ `#dccfbc` (тёплый пастельный бежевый)

**В4. Сколько вопросов в FAQ?**
→ 4 вопроса

### Группа 2: Кнопка темы и FAQ

**В5. Как выглядит кнопка переключения темы?**
→ Иконка Солнце/Луна (Lucide: `Sun` / `Moon`)

**В6. FAQ: можно ли раскрыть несколько ответов одновременно?**
→ Только один. При клике на другой — предыдущий закрывается.

**В7. Автопрокрутка в блоке отзывов?**
→ Нет, только стрелками.

**В8. Эффект тумана — с какой стороны?**
→ Только снизу.

### Группа 3: Счётчик и отзывы

**В9. Какой шрифт для цифр счётчика?**
→ Inter (уже есть в проекте). Вместо Playfair Display — строгий, цифры одной высоты.

**В10. Насколько медленнее счётчик?**
→ 3 секунды (сейчас 2).

**В11. Скорость анимации смены темы?**
→ 1.2 секунды.

**В12. Показывать точки-индикатор под отзывами?**
→ Да, золотые точки (текущий — яркий, остальные — приглушённые).

### Группа 4: Стрелки, текст, мобилка, FAQ фон

**В13. Как выглядят стрелки отзывов?**
→ Круглые кнопки с `‹ ›` — золотой border + золотая стрелка.

**В14. Текст второго блока (Партнёры) — насколько сократить?**
→ ~50%. Тот же стиль, просто меньше слов.

**В15. Кнопка темы на мобилке — где?**
→ В navbar рядом с иконкой телефона (перед кнопкой меню).

**В16. Фон блока FAQ?**
→ Светлый (как ServicesSection — белый фон).

### Группа 5: Шрифт, скорость, тема, слайдер

**В17. Анимация перелистывания отзывов?**
→ Fade — плавное появление/исчезновение.

**В18. ScrollSnapper — что сделать?**
→ Убрать полностью. Стандартный скролл без snap.

**В19. Оптимизация мобилки — приоритеты?**
→ Всё равнозначно важно.

**В20. Стиль сокращённого текста партнёров?**
→ Тот же стиль, просто меньше слов.

### Группа 6: Стиль кнопки, FAQ детали

**В21. Стиль кнопки переключения темы?**
→ Круглая, золотой border, чёрный фон (`rounded-full border border-gold bg-charcoal text-gold`).

**В22. Hover-эффект на кнопке вопроса FAQ?**
→ Да, gold border при hover.

**В23. Скорость анимации открытия FAQ?**
→ 0.5 секунды.

**В24. FAQ в мобильном меню?**
→ Да, добавить как «Q&A» (короче для узкого экрана).

### Группа 7: localStorage, тёмные секции, мобилка

**В25. Сохранять тему в localStorage?**
→ Да, запоминать выбор пользователя.

**В26. Как тёмные секции ведут себя в светлой теме?**
→ В светлой теме тёмные секции (Hero, Testimonials и т.д.) плавно светлеют до `#dccfbc`, а элементы с фоном `#626260` плавно светлеют до белого.

**В27. Отзывы на мобилке — сколько в ряд?**
→ 1 отзыв на весь экран. Стрелки по бокам.

**В28. Валюта в блоке «Результаты»?**
→ `whitespace-nowrap` + уменьшить font-size — число и валюта остаются в одной строке.

### Группа 8: Логотип, FAQ позиция, скорость счётчика

**В29. Логотип в navbar при светлой теме?**
→ Остаётся белым. Navbar всегда тёмный.

**В30. Ответ FAQ — где появляется?**
→ Снизу под кнопкой-вопросом (эффект штора вниз).

**В31. Счётчик перезапускается при смене темы?**
→ Нет. Запускается только один раз при входе в зону видимости.

**В32. Форма контактов в светлой теме?**
→ ContactSection уже светлая — ничего не менять.

### Группа 9: Footer, Sticky, туман, тексты

**В33. Footer меняет цвет при смене темы?**
→ Остаётся чёрным (как navbar). Всегда тёмный.

**В34. StickyCtaButton — оставить?**
→ Да, оставить как есть.

**В35. Цвет тумана?**
→ Совпадает с фоном текущей секции (белый/бежевый — как переменная `--light-section-bg`).

**В36. Генерация текстов (отзывы, FAQ, текст партнёров)?**
→ Агент генерирует сам, ориентируясь на существующий стиль.

### Группа 10: Порядок работы и проверка

**В37. С чего начать?**
→ Сначала GitHub бэкап (тег v1.0), потом задачи по порядку.

**В38. Как проверяем результат?**
→ Playwright MCP + e2e-tester субагент.

**В39. Коммиты в процессе?**
→ Локальный коммит после каждой задачи, без git push.

**В40. Есть ли дополнительные замечания?**
→ Нет. Но хочу сначала одобрить план.

---

## Порядок задач

### Задача 0 — GitHub резервная копия (первое что делаем)
```bash
# Запушить все изменения
git add <все изменённые файлы>
git commit -m "Финальная версия сайта перед косметикой"
git push origin main
git tag v1.0
git push origin v1.0
```
**После:** больше `git push` не делаем. Только локальные коммиты.

---

### Задача 1 — Убрать жёлтое свечение за курсором ✅ ВЫПОЛНЕНО
- **Файл:** `src/app/layout.tsx`
- Удалить импорт `CursorBlob` и JSX `<CursorBlob />`
- Файл `src/components/ui/CursorBlob.tsx` не трогать
- **Коммит:** f0ccd23

---

### Задача 2 — Сократить текст второго блока (~50%) ✅ ВЫПОЛНЕНО
- **Файлы:** `src/lib/constants.ts` (объект PARTNERS)
- Сокращено поле `bio` каждого партнёра ~50%, стиль сохранён
- **Нюанс:** PartnersSection.tsx не менялся — текст только в constants.ts (через PartnersTabs.tsx)
- **Коммит:** f0ccd23

---

### Задача 3 — Убрать ScrollSnapper ✅ ВЫПОЛНЕНО
- **Файл:** `src/app/layout.tsx` — удалён импорт и `<ScrollSnapper />`
- Якорная навигация (`scroll-margin-top: 64px` в globals.css) — оставлена
- `scroll-behavior: smooth` — оставлен
- **Коммит:** f0ccd23

---

### Задача 4 — Переключатель тёмной/светлой темы ⭐ (самая сложная) — ✅ ВЫПОЛНЕНО

**Нюансы реализации:**
- Использовался `.transitions-enabled` класс на `<html>` с задержкой 50ms после mount — предотвращает flash при первой загрузке
- В Tailwind v4 `@theme inline` — все токены ссылаются через `var()`, переключение через `[data-theme="dark"]` на html-элементе
- Два инстанса ThemeToggle (desktop fixed + mobile navbar) синхронизируются через кастомный `window.dispatchEvent("theme-change")` event — не через Context
- Hydration mismatch предотвращён через `mounted` флаг в ThemeToggle — до mount рендерится нейтральный placeholder без иконки
- Inline скрипт в `<head>` устанавливает data-theme до гидратации — обёрнут в try/catch для Safari private mode
- Navbar изменён с `bg-transparent` на `bg-charcoal/75 backdrop-blur-sm` при scrolled=false — иначе белый текст нечитаем поверх бежевого Hero в светлой теме
- Все тёмные секции (Hero, Partners, Cases, HowWeWork, Testimonials, Clients) адаптированы через CSS переменные `--dark-text-100/80/70/40/10` — исправлено ~20 хардкодных `text-white`
- PartnersTabs — градиент на фото партнёра переведён на `var(--dark-section-bg)`, текст имён/описаний на CSS переменные
- Footer, BottomNav, MobileMenu — намеренно оставлены всегда тёмными (согласно плану: "Navbar/Footer всегда тёмный")
- CaseCard карточки (`bg-charcoal-soft`) — оставлены постоянно тёмными как декоративный элемент, это нормально визуально
- **Коммит:** выполнено в рамках задачи 4

**Дополнительная косметика светлой темы — ✅ ВЫПОЛНЕНО**
- `--card-bg` → `#dccfbc` (бежевые карточки в блоках Результаты, Практика, О компании, Новости, Контакты)
- `CaseCard` → `--dark-card-bg` (`#ffffff`) — белые карточки в блоке Кейсы
- Кавычки в блоке Отзывы → `--dark-text-100` (чёрные)
- Обводка круга с цифрой в блоке Процесс → `--dark-text-100` (чёрная)
- Текст "Обсудить вашу ситуацию" и стрелка в блоке Кейсы → `--dark-text-100` (чёрные)
- Категория и сумма в карточках Кейсов → `--dark-text-100` (чёрные, убрано золото)
- Текст "Партнёры компании выступают экспертами..." → `#0a0a0a` (всегда чёрный)

**Дополнительная косметика тёмной темы — ✅ ВЫПОЛНЕНО**
- `--card-bg` → `#0a0a0a` (чёрные карточки в блоках Результаты, Практика, О компании, Новости, Контакты)
- `--card-accent` → gold (иконки и заголовки карточек — золотые)
- `--stat-color` → gold (цифры и подписи в блоке Результаты — золотые)
- `--stat-card-border/shadow` → золотое свечение карточки Результатов
- `--service-card-border/shadow` → золотое свечение карточек Практики
- `--case-card-border/shadow` → золотое свечение + золотые заголовки карточек Кейсов
- `--dark-card-bg` → `#3a3a3a` (серые карточки в блоках Кейсы и Отзывы)
- `--principle-card-border/shadow` → золотое свечение карточек О компании
- `--news-card-border/shadow` → золотое свечение карточек Новостей
- `--clients-card-border/shadow` → золотое свечение карточки Клиентов
- `--partner-label/tab-active/tab-muted` → золотые заголовки и имена в блоке Команда
- `--process-step-color` → gold (обводка, цифры и заголовки шагов в блоке Процесс)
- `--quote-color` → gold (кавычки в блоке Отзывы)
- `[data-theme="dark"] #contact, #clients, #services` → `--muted-foreground: #0a0a0a` (серые тексты → чёрные)
- Школа Юристов Разумовских → `--card-accent` (золотой заголовок в блоке Клиенты)

---

### Задача 4 — Переключатель тёмной/светлой темы ⭐ (самая сложная)

#### Логика тем

| Тема | Белые/светлые блоки | Тёмные блоки (Hero, Testimonials) |
|---|---|---|
| Светлая (по умолчанию) | `#ffffff` | `#dccfbc` + тёмный текст `#3d3326` |
| Тёмная | `#626260` + тёмный текст | `#0a0a0a` + белый текст |

⚠️ **Нюанс:** в светлой теме на фоне `#dccfbc` белый текст не читается → нужно менять `color` внутри тёмных секций.

#### CSS переменные (`globals.css`)
```css
:root, [data-theme="light"] {
  --dark-section-bg:   #dccfbc;
  --dark-section-text: #3d3326;
  --light-section-bg:  #ffffff;
  --light-section-text: #0a0a0a;
}

[data-theme="dark"] {
  --dark-section-bg:   #0a0a0a;
  --dark-section-text: #ffffff;
  --light-section-bg:  #626260;
  --light-section-text: #1a1a1a;
}

/* Плавная анимация для всех элементов */
*, *::before, *::after {
  transition: background-color 1.2s ease, color 1.2s ease, border-color 1.2s ease;
}
```

#### Применение в компонентах
`SectionWrapper.tsx` принимает пропс `dark`:
- `dark={true}` → `style={{ background: 'var(--dark-section-bg)', color: 'var(--dark-section-text)' }}`
- `dark={false}` → `style={{ background: 'var(--light-section-bg)', color: 'var(--light-section-text)' }}`

Блоки с `bg-white` инлайн → заменить на `bg-[var(--light-section-bg)]`

#### Новые файлы
- `src/hooks/useTheme.ts` — хук: localStorage + `document.documentElement.dataset.theme`
- `src/components/ui/ThemeToggle.tsx` — кнопка: `w-10 h-10 rounded-full border border-gold bg-charcoal`, иконки `Sun`/`Moon` из Lucide

#### Размещение кнопки
- **Desktop:** `fixed bottom-6 left-6 z-50` в `layout.tsx`
- **Mobile:** в `Navbar.tsx` рядом с иконкой телефона

#### Исключения (не реагируют на тему)
- Navbar → всегда тёмный
- Footer → всегда тёмный
- Логотип → всегда белый

---

### Задача 5 — Расширить отзывы + слайдер (7 штук) ✅ ВЫПОЛНЕНО

**Реализованные файлы:**
- `src/lib/constants.ts` — добавлены отзывы 4-7 (девелопер, инвестор, экспортёр, директор предприятия)
- `src/components/sections/TestimonialsSection.tsx` — полностью переписан

**Нюансы реализации:**
- Desktop: CSS-трек — все 7 карточек в одной горизонтальной полосе, `overflow: hidden`, анимация `x` трека через Framer Motion (`0.45s easeInOut`). Ширина карточек измеряется через `ResizeObserver` (нужен JS для точного расчёта с учётом gap). Карточки вне окна плавно гаснут (`opacity: 0`, `0.35s`).
- Mobile: `AnimatePresence mode="popLayout"` + направленный слайд (`x: ±100%`, `0.45s`)
- Стрелки и точки: `--dark-text-100` (чёрный/белый в зависимости от темы), `disabled:cursor-not-allowed disabled:opacity-30`
- Все цвета через CSS переменные: `--dark-card-bg`, `--dark-text-100`, `--dark-text-70`, `--quote-color`
- Первоначальные подходы с `AnimatePresence + layout + popLayout` отброшены — конфликт `layout` и `x` анимации давал визуальные артефакты

---

### Задача 6 — Блок FAQ ✅ ВЫПОЛНЕНО

**Реализованные файлы:**
- `src/components/sections/FAQSection.tsx` — новый файл
- `src/app/page.tsx` — `<FAQSection />` добавлен перед `<ContactSection />`
- `src/components/layout/Navbar.tsx` — `{ label: "FAQ", href: "#faq", mobileLabel: "Q&A" }` между «Клиенты» и «Контакты», gap-3 → gap-2
- `src/components/layout/MobileMenu.tsx` — `NavLink.mobileLabel?: string`, рендер `link.mobileLabel ?? link.label`
- `src/app/globals.css` — добавлены `--faq-card-border/shadow/active-border/active-shadow`

**Нюансы реализации:**
- Аккордеон через Framer Motion `height: 0 → "auto"` (`0.5s easeInOut`) — анимирует до реальной высоты контента, открытие и закрытие симметричны
- Стиль карточек через `--card-bg` (бежевый/чёрный), текст через `--dark-text-100/70`, акцент через `--card-accent` (gold в тёмной)
- При открытии карточка получает `--faq-card-active-border` (gold) и `--faq-card-active-shadow`
- В светлой теме: бежевые карточки на белом; в тёмной: чёрные карточки с золотым свечением
- `bun run build` — ошибок нет ✓

---

### Задача 7 — Оптимизация StatCounter ✅ ВЫПОЛНЕНО

**Файл:** `src/components/ui/StatCounter.tsx`

- Цифры: заменить `font-display` → `font-sans` (Inter) на элементе с числом
- Добавить `tabular-nums tracking-tight` для выравнивания цифр
- Скорость: в `src/hooks/useCountUp.ts` изменить дефолтную `duration: 2000` → `3000`
- Валюта: обернуть `{value}{suffix} {unit}` в `<span className="whitespace-nowrap">`

---

### Задача 9 — Мобильная оптимизация (финальная проверка) ✅ ВЫПОЛНЕНО

- **Stats валюта:** уже исправлено в задаче 7
- **Кнопка темы в navbar:** уже в задаче 4
- **Проверить все секции через Playwright:**
  - Hero: заголовок и CTA читаемы
  - Stats: цифры в одну строку, без переноса
  - Отзывы: 1 карточка, стрелки по бокам
  - FAQ: аккордеон работает
  - Navbar: FAQ/Q&A и кнопка темы видны

---

## Критические файлы

| Файл | Задачи |
|---|---|
| `src/app/layout.tsx` | 1, 3, 4 |
| `src/app/globals.css` | 4 |
| `src/app/page.tsx` | 6 |
| `src/lib/constants.ts` | 2, 5, 6 |
| `src/components/layout/Navbar.tsx` | 4 (mobile toggle), 6 (FAQ link) |
| `src/components/layout/MobileMenu.tsx` | 6 (Q&A link) |
| `src/components/sections/PartnersSection.tsx` | 2 |
| `src/components/sections/TestimonialsSection.tsx` | 5 |
| `src/components/ui/SectionWrapper.tsx` | 4 |
| `src/components/ui/StatCounter.tsx` | 7, 9 |
| `src/hooks/useCountUp.ts` | 7 |

**Новые файлы:**
- `src/hooks/useTheme.ts`
- `src/components/ui/ThemeToggle.tsx`
- `src/components/sections/FAQSection.tsx`

---

## Агенты, скилы, правила

| Когда | Что использовать |
|---|---|
| Перед задачей 4 | `docs` агент — Tailwind v4 CSS variables + data-theme pattern |
| После каждой задачи | `commit` скилл — локальный коммит (git add + git commit, БЕЗ push) |
| После всех задач | `reviewer` агент — независимая проверка |
| Финальная проверка | Playwright MCP + `e2e-tester` субагент |
| Правило Plan→Do→Verify | Проверять каждую задачу перед следующей |

---

## Чеклист проверки

- [x] GitHub тег `v1.0` создан, дальнейших `git push` нет
- [x] Жёлтое свечение за курсором отсутствует
- [x] Текст PartnersSection сократился ~50%, стиль сохранён
- [x] Прокрутка мышью плавная, не защёлкивается на секциях
- [x] Кнопка темы: desktop левый нижний + mobile navbar
- [x] Светлая тема по умолчанию; тёмные секции имеют фон `#dccfbc`
- [x] Тёмная тема: белые секции имеют фон `#626260`
- [x] Переключение плавное 1.2 сек; localStorage сохраняет выбор
- [x] Счётчик: Inter шрифт, цифры одной высоты, 3 сек анимация
- [x] Отзывы: 7 штук, 3 видимо на десктопе, 1 на мобилке, fade, стрелки, точки
- [x] FAQ: 4 вопроса, аккордеон один за раз, стрелка ↓, 0.5 сек
- [x] FAQ в navbar между «Клиенты» и «Контакты»; Q&A в мобильном меню
- [x] Мобилка: валюта в одну строку, кнопка темы видна, FAQ работает
- [x] `bun run build` — нет ошибок TypeScript
