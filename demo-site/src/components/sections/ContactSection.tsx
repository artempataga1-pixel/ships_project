import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function ContactSection() {
  return (
    <SectionWrapper id="contact">
      <p className="text-muted-foreground text-sm">ContactSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
