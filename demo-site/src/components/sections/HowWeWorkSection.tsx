import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function HowWeWorkSection() {
  return (
    <SectionWrapper id="process">
      <p className="text-muted-foreground text-sm">HowWeWorkSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
