import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function ClientsSection() {
  return (
    <SectionWrapper id="clients" dark>
      <p className="text-white/20 text-sm">ClientsSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
