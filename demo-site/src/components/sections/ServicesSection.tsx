import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function ServicesSection() {
  return (
    <SectionWrapper id="services">
      <p className="text-muted-foreground text-sm">ServicesSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
