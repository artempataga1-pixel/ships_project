import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function PartnersSection() {
  return (
    <SectionWrapper id="partners">
      <p className="text-muted-foreground text-sm">PartnersSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
