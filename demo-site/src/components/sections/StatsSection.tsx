import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function StatsSection() {
  return (
    <SectionWrapper id="stats">
      <p className="text-muted-foreground text-sm">StatsSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
