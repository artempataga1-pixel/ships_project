import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function GeographySection() {
  return (
    <SectionWrapper id="geography">
      <p className="text-muted-foreground text-sm">GeographySection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
