import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function NewsSection() {
  return (
    <SectionWrapper id="news">
      <p className="text-muted-foreground text-sm">NewsSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
