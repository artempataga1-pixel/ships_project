import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function RecommendSection() {
  return (
    <SectionWrapper id="recommend">
      <p className="text-muted-foreground text-sm">RecommendSection — реализация в шаге 4</p>
    </SectionWrapper>
  );
}
