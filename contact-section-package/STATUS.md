# Статус

**Нужно определиться: ставить этот пакет в проект или нет.**

Справочно: сейчас `src/components/sections/ContactsSection.tsx` уже использует
разметку/стили этого пакета как источник — блок перенесён в React вручную.
Сам архив (`contact-section.html`, `css/`, `js/`, `demo.html`) как отдельный
модуль в проект не подключён, лежит только для справки/сверки.
